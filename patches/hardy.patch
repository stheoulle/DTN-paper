diff --git a/asabr-routing/src/config.rs b/asabr-routing/src/config.rs
index cc3de6b1..a77aa801 100644
--- a/asabr-routing/src/config.rs
+++ b/asabr-routing/src/config.rs
@@ -1,3 +1,4 @@
+use std::collections::HashMap;
 use std::path::PathBuf;
 
 #[derive(Debug, Clone)]
@@ -6,4 +7,19 @@ pub struct Config {
     pub router: String,
     pub contact_plan_path: PathBuf,
     pub local_node_id: hardy_bpv7::eid::NodeId,
+    /// Maps EID node-ID strings (e.g. "dtn://bob.dtn/") to A-SABR numeric node IDs.
+    /// Required for any non-IPN destination that A-SABR v1 cannot resolve on its own.
+    pub eid_map: HashMap<String, u16>,
+}
+
+impl Default for Config {
+    fn default() -> Self {
+        Self {
+            protocol_id: String::new(),
+            router: String::new(),
+            contact_plan_path: PathBuf::new(),
+            local_node_id: hardy_bpv7::eid::NodeId::LocalNode,
+            eid_map: HashMap::new(),
+        }
+    }
 }
diff --git a/asabr-routing/src/provider.rs b/asabr-routing/src/provider.rs
index 5132c8c8..db23427c 100644
--- a/asabr-routing/src/provider.rs
+++ b/asabr-routing/src/provider.rs
@@ -1,9 +1,10 @@
+use std::collections::HashMap;
 use std::sync::Mutex;
 use std::thread::JoinHandle;
 
 use async_trait::async_trait;
 use hardy_bpa::routes::{self, LiveRoutingProvider};
-use hardy_bpv7::eid::{Eid, IpnNodeId};
+use hardy_bpv7::eid::{Eid, IpnNodeId, NodeId};
 use tokio::sync::{mpsc, oneshot};
 use tracing::debug;
 
@@ -22,6 +23,8 @@ enum Command {
 
 pub struct AsabrRoutingProvider {
     local_node_id: u16,
+    eid_map: HashMap<String, u16>,
+    reverse_eid_map: HashMap<u16, NodeId>,
     tx: mpsc::UnboundedSender<Command>,
     handle: Mutex<Option<JoinHandle<()>>>,
 }
@@ -31,6 +34,18 @@ impl AsabrRoutingProvider {
         let (init_tx, init_rx) = std::sync::mpsc::sync_channel::<Result<u16, Error>>(0);
         let (tx, rx) = mpsc::unbounded_channel::<Command>();
 
+        // Build reverse map: A-SABR node number → NodeId (for returning correct next-hop EID)
+        let mut reverse_eid_map = HashMap::<u16, NodeId>::new();
+        for (eid_str, &node_num) in &config.eid_map {
+            if let Ok(eid) = eid_str.parse::<Eid>() {
+                if let Ok(node_id) = eid.to_node_id() {
+                    reverse_eid_map.insert(node_num, node_id);
+                }
+            }
+        }
+
+        let eid_map = config.eid_map.clone();
+
         let handle = std::thread::Builder::new()
             .name("hardy-asabr-router".into())
             .spawn(move || worker_main(config, init_tx, rx))
@@ -50,6 +65,8 @@ impl AsabrRoutingProvider {
 
         Ok(Self {
             local_node_id,
+            eid_map,
+            reverse_eid_map,
             tx,
             handle: Mutex::new(Some(handle)),
         })
@@ -139,7 +156,7 @@ impl LiveRoutingProvider for AsabrRoutingProvider {
         let destination = &bundle.bundle.destination;
         debug!("A-SABR routing lookup for {destination}");
 
-        let translated = translate::translate_bundle(bundle, self.local_node_id)
+        let translated = translate::translate_bundle(bundle, self.local_node_id, &self.eid_map)
             .map_err(|e| {
                 debug!("A-SABR translation failed for {destination}: {e}");
                 routes::Error::Internal(Box::new(e))
@@ -161,10 +178,17 @@ impl LiveRoutingProvider for AsabrRoutingProvider {
                 routes::Error::Internal(Box::new(e))
             })?;
         let next_hop = result.map(|node| {
-            Eid::from(IpnNodeId {
-                allocator_id: 0,
-                node_number: u32::from(node),
-            })
+            // Prefer the mapped EID (e.g. dtn://bob.dtn/) so the CLA lookup matches
+            // the peer's registered node-id.  Fall back to synthesised ipn:0.X.0 for
+            // peers that were configured with IPN EIDs.
+            if let Some(node_id) = self.reverse_eid_map.get(&node) {
+                Eid::from(node_id.clone())
+            } else {
+                Eid::from(IpnNodeId {
+                    allocator_id: 0,
+                    node_number: u32::from(node),
+                })
+            }
         });
         match &next_hop {
             Some(eid) => debug!("A-SABR returned next-hop {eid} for {destination}"),
@@ -237,6 +261,7 @@ mod tests {
             router: "SpsnHybridParenting".into(),
             contact_plan_path: plan_path,
             local_node_id: "ipn:1.0".parse().unwrap(),
+            eid_map: Default::default(),
         }
     }
 
diff --git a/asabr-routing/src/translate.rs b/asabr-routing/src/translate.rs
index 692fe6de..1053efd4 100644
--- a/asabr-routing/src/translate.rs
+++ b/asabr-routing/src/translate.rs
@@ -1,3 +1,5 @@
+use std::collections::HashMap;
+
 use hardy_bpv7::eid::NodeId;
 
 use crate::Error;
@@ -5,6 +7,7 @@ use crate::Error;
 pub(crate) fn translate_bundle(
     bundle: &hardy_bpa::bundle::Bundle,
     local_node_id: u16,
+    eid_map: &HashMap<String, u16>,
 ) -> Result<a_sabr::bundle::Bundle, Error> {
     let destination = bundle
         .bundle
@@ -12,10 +15,15 @@ pub(crate) fn translate_bundle(
         .to_node_id()
         .map_err(Error::InvalidDestinationNodeId)?;
 
-    let destination = match destination {
+    let destination = match &destination {
         NodeId::Ipn(ipn) if ipn.allocator_id == 0 => u16::try_from(ipn.node_number)
             .map_err(|_| Error::NodeIdOutOfRange(ipn.node_number))?,
-        other => return Err(Error::UnsupportedNodeId(other)),
+        other => {
+            let key = other.to_string();
+            *eid_map
+                .get(&key)
+                .ok_or_else(|| Error::UnsupportedNodeId(other.clone()))?
+        }
     };
 
     Ok(a_sabr::bundle::Bundle {
@@ -33,6 +41,8 @@ pub(crate) fn now_asabr_time() -> f64 {
 
 #[cfg(test)]
 mod tests {
+    use std::collections::HashMap;
+
     use super::*;
 
     fn make_bundle(destination: &str) -> hardy_bpa::bundle::Bundle {
@@ -61,7 +71,7 @@ mod tests {
     fn translates_ipn_bundle_to_asabr_bundle() {
         let bundle = make_bundle("ipn:0.8.7");
 
-        let translated = translate_bundle(&bundle, 1).unwrap();
+        let translated = translate_bundle(&bundle, 1, &HashMap::new()).unwrap();
 
         assert_eq!(translated.source, 1);
         assert_eq!(translated.destinations, vec![8]);
@@ -70,21 +80,31 @@ mod tests {
     }
 
     #[test]
-    fn translator_rejects_dtn_destination() {
+    fn translator_rejects_dtn_destination_without_map() {
         let bundle = make_bundle("dtn://mars/svc");
 
-        match translate_bundle(&bundle, 1) {
+        match translate_bundle(&bundle, 1, &HashMap::new()) {
             Err(Error::UnsupportedNodeId(_)) => {}
             Err(other) => panic!("expected UnsupportedNodeId, got {other:?}"),
             Ok(_) => panic!("expected translation error, got Ok"),
         }
     }
 
+    #[test]
+    fn translator_resolves_dtn_destination_via_map() {
+        let mut map = HashMap::new();
+        map.insert("dtn://mars/".to_string(), 5u16);
+        let bundle = make_bundle("dtn://mars/svc");
+
+        let translated = translate_bundle(&bundle, 1, &map).unwrap();
+        assert_eq!(translated.destinations, vec![5]);
+    }
+
     #[test]
     fn translator_rejects_ipn_destination_above_u16_range() {
         let bundle = make_bundle("ipn:0.70000.7");
 
-        match translate_bundle(&bundle, 1) {
+        match translate_bundle(&bundle, 1, &HashMap::new()) {
             Err(Error::NodeIdOutOfRange(70000)) => {}
             Err(other) => panic!("expected NodeIdOutOfRange(70000), got {other:?}"),
             Ok(_) => panic!("expected translation error, got Ok"),
diff --git a/bpa-server/src/config/asabr.rs b/bpa-server/src/config/asabr.rs
index dbd9fe9f..0bb890ed 100644
--- a/bpa-server/src/config/asabr.rs
+++ b/bpa-server/src/config/asabr.rs
@@ -1,3 +1,4 @@
+use std::collections::HashMap;
 use std::path::PathBuf;
 
 use serde::{Deserialize, Serialize};
@@ -27,6 +28,10 @@ pub struct Config {
     pub router: String,
     pub contact_plan_path: PathBuf,
     pub local_node_id: hardy_bpv7::eid::NodeId,
+    /// Maps EID node-ID strings to A-SABR node numbers.
+    /// Required for non-IPN destinations (e.g. dtn://bob.dtn/ → 3).
+    #[serde(default)]
+    pub eid_map: HashMap<String, u16>,
 }
 
 impl Default for Config {
@@ -36,6 +41,7 @@ impl Default for Config {
             router: default_router(),
             contact_plan_path: default_contact_plan_path(),
             local_node_id: default_local_node_id(),
+            eid_map: HashMap::new(),
         }
     }
 }
@@ -47,6 +53,7 @@ impl From<&Config> for hardy_asabr_routing::Config {
             router: config.router.clone(),
             contact_plan_path: config.contact_plan_path.clone(),
             local_node_id: config.local_node_id.clone(),
+            eid_map: config.eid_map.clone(),
         }
     }
 }
diff --git a/bpa-server/src/config/cla.rs b/bpa-server/src/config/cla.rs
index 5f30cae2..8aba6e79 100644
--- a/bpa-server/src/config/cla.rs
+++ b/bpa-server/src/config/cla.rs
@@ -70,6 +70,7 @@ impl Config {
                 );
                 Ok(None)
             }
+            #[cfg(feature = "cspcl")]
             ClaType::Cspcl(config) => {
                 let cla =
                     Arc::new(hardy_cspcl::Cla::new(config).map_err(|e| {
