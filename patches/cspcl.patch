diff --git a/ud3tn-integration/dev.patch b/ud3tn-integration/dev.patch
index d373dff..a362fda 100644
--- a/ud3tn-integration/dev.patch
+++ b/ud3tn-integration/dev.patch
@@ -27,8 +27,8 @@ index b109adb..a33110c 100644
                       -Iexternal/tinycbor/src \
                       -Iexternal/util/include \
 +                     -Iexternal/cspcl \
-+                     -I/home/mathias/libcsp-src/include \
-+                     -I/home/mathias/libcsp-src/build/include \
++                     -I__DTN_ROOT__/libcsp/include \
++                     -I__DTN_ROOT__/libcsp/build/include \
                       -Igenerated
  
  $(eval $(call addComponentWithRules,components/aap))
@@ -53,7 +53,7 @@ index 2adaa41..469dd6c 100644
  LDFLAGS += -lpthread
  
 +# libcsp for CSP CLA
-+LDFLAGS += -L/home/mathias/libcsp-src/build -lcsp -lzmq -lsocketcan
++LDFLAGS += -L__DTN_ROOT__/libcsp/build -lcsp -lzmq -lsocketcan
 +
 +
  ifndef DISABLE_SQLITE_STORAGE
diff --git a/rust-bindings/cspcl/Cargo.toml b/rust-bindings/cspcl/Cargo.toml
index 89d5dc8..4b68d00 100644
--- a/rust-bindings/cspcl/Cargo.toml
+++ b/rust-bindings/cspcl/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "cspcl"
-version = "0.4.0"
+version = "0.6.2"
 edition.workspace = true
 description = "Rust bindings for the cspcl library"
 authors = ["cspcl contributors"]
@@ -9,6 +9,13 @@ repository = "https://github.com/d3tn/cspcl"
 readme = "README.md"
 keywords = ["cspcl"]
 
+[features]
+# tokio support is unconditional in this crate (unlike the published
+# crates.io version, which gates it behind this feature). Kept as a
+# no-op so callers requesting it (e.g. hardy's `cspcl-bindings`
+# dependency) don't fail feature resolution.
+async-tokio = []
+
 [dependencies]
 bytes = "1.12.0"
 cspcl-sys = { version = "0.3", path = "../cspcl-sys" }
diff --git a/rust-bindings/cspcl/src/address.rs b/rust-bindings/cspcl/src/address.rs
index 443aa62..493cc27 100644
--- a/rust-bindings/cspcl/src/address.rs
+++ b/rust-bindings/cspcl/src/address.rs
@@ -13,7 +13,9 @@ impl TryFrom<Bytes> for CspAddress {
 
     fn try_from(value: Bytes) -> Result<Self, Self::Error> {
         let mut raw_addr = value.into_iter();
-        raw_addr.len().eq(&2).ok_or(ParseAddress)?;
+        if raw_addr.len() != 2 {
+            return Err(ParseAddress);
+        }
         let addr = raw_addr.next().ok_or(ParseAddress)?;
         let port = raw_addr.next().ok_or(ParseAddress)?;
         Ok(Self { addr, port })
